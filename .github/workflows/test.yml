name: Dynamic Testing

on:
  push:
    branches:
      - release-*

jobs:
  test:
    name: test em man
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1

    - name: json checker
      id: json
      uses: minkimcello/script-detector@master

    - run: yarn 

    - name: testing
      if: ${#steps.json.test} > 0
      run: echo this works man