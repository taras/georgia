name: on pr test

on:
  pull_request:
    branches:
      # - features-*
      # - release-*
      - placeholder

jobs:
  publish: 
    name: Publish Commit Package
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: georgia/future-refactor test
      uses: minkimcello/georgia/.github/future-preview-refactor@release-1.0.0
      env:
        NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}